#!/bin/sh

#
# Configuration file for the jenkins administration product (jenkins_admin)
#

if [ "${MGICONFIG}" = "" ]
then
    MGICONFIG=/usr/local/mgi/live/mgiconfig
    export MGICONFIG
fi

. ${MGICONFIG}/master.config.sh

PUBLIC_WEBSERVER=bhmgipub01lp

export PUBLIC_WEBSERVER
